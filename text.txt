/* --- ГЛОБАЛЬНАЯ ПОЧИНКА СЕТКИ КАТАЛОГА --- */

/* 1. Делаем сетку товаров из 3-х колонок (вместо одного кривого столба) */
.catalog-cards {
    display: grid !important;
    grid-template-columns: repeat(3, 1fr) !important;
    gap: 24px !important;
    align-items: start !important;
}

/* 2. Расталкиваем активные фильтры и сортировку по краям */
.active-filters {
    display: flex;
    align-items: center;
    gap: 10px;
    margin-bottom: 20px;
}

/* 3. Делаем так, чтобы кнопка сортировки красиво стояла справа над товарами */
.catalog-right-sort {
    display: flex;
    justify-content: flex-end;
    margin-bottom: 20px;
    width: 100%;
}
